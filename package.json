{
  "name": "cf-cursor-model-id",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "tsc --noEmit",
    "compile": "tsc -p tsconfig.json",
    "test": "npm run compile && node --test dist/test/index.test.js"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "typescript": "^5.6.3",
    "wrangler": "^4.0.0"
  }
}
